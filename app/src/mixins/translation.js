import {
    tPokemon, tMove, tItem, tNature,
    tAbility, tGen, tWeather
} from "../i18n";

export default {
    methods: {
        $tPokemon: tPokemon,
        $tMove: tMove,
        $tItem: tItem,
        $tNature: tNature,
        $tAbility: tAbility,
        $tGen: tGen,
        $tWeather: tWeather
    }
};
